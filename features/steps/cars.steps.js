Given(/^we are in the main travels page$/, function() {
  return "pending";
});
When(/^the required information is set$/, function() {
  return "pending";
});
Then(/^we return the best car option to rent$/, function() {
  return "pending";
});