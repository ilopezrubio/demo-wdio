Given("that a user is on the main page", function () {
  return "pending";
});

When("the user looks for a flight", function () {
  return "pending";
});

Then("the user selects a flight to book ", function () {
  return "pending";
});
